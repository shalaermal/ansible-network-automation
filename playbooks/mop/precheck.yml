- hosts: access_switches
  gather_facts: no
  tasks:
    - import_tasks: ../../roles/vlan_lifecycle/tasks/precheck.yml

- hosts: routers
  gather_facts: no

  tasks:
    - name: Check interface status
      cisco.ios.ios_command:
        commands:
          - show ip interface brief
